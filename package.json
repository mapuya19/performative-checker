{
  "name": "performative-checker",
  "version": "1.0.0",
  "private": true,
  "description": "Webcam-based performative checker using TF.js COCO-SSD",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "start": "tsc && node dist/server.js",
    "start:port": "PORT=8081 node dist/server.js",
    "dev": "tsc && tsc --watch & node dist/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^25.3.3",
    "typescript": "^5.9.3"
  }
}
